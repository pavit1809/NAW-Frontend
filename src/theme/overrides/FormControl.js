// ----------------------------------------------------------------------

// eslint-disable-next-line no-unused-vars
export default function FormControl(theme) {
  return {
    MuiFormControl: {
      styleOverrides: {
        root: {
          margin: 0,
        }
      }
    },
  };
}
